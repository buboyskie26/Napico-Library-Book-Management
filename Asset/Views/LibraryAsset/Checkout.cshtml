@model Asset.ViewModel.AssetVM.CheckoutIndex;


<div class="container mt-0">
        @* <div class="row">
            <div class="row col-md-4">

                <div>
                    <h3 style="font-size:17px;">@Model.Title</h3>
                    <img src="@Model.ImageUrl" style="width:250px; padding:10px;" />
                </div>
            </div>

            </div>

        <div class="row col-md-8">
                <form method="post" asp-action="PlaceCheckout">
                    <input type="hidden" asp-for="AssetId" />
                     <div class="form-group">
            Enter Library Card Id
            <input type="text" asp-for="UserCardId" />
        </div>
        <button class="btn btn-success">Submit</button>
                </form>
            </div>*@
        <form method="post" asp-action="PlaceCheckout">
            <div class="img-row">
                <div class="img-container" style="border-radius:20px;">
                    <div class="img-div" style="background-image:url(@Model.ImageUrl);">

                        <button class="photos">PHOTOS</button>
                        <p class="cost">$@Model.Cost.ToString("F")</p>
                    </div>
                    <div class="img-content">
                        <h3>@Model.Title</h3>
                        <p class="desc mt-2 mono-lg">
                            @Model.Description
                        </p>
                    </div>
                    <div class="form-group text-center mono-slim">
                        <b>Enter Library Card Id: </b>
                        <input class="ml-2" style="border-radius:12px;" type="text" asp-for="UserCardId" />
                    </div>
                    <input type="hidden" asp-for="AssetId" />
                    <button style="font-size: 19px; background-color:#2584ff;" class="btn btn-checkout">Submit</button>

                </div>

            </div>
          
          


        </form>
    </div>
